paths:
  /:
    get:
      tags:
        - health
      description: health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: string
